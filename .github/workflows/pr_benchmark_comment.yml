name: pr_benchmark_comment

on:
  workflow_run:
    workflows: [pr_benchmarks]
    types:
      - completed

jobs:
  comment_pr:
    uses: cvxpy/actions/.github/workflows/pr_benchmark_comment.yml@cfcc3f4309e2c50221f21ff4070be3a9de4ccdf1 # main as of 2026-07-07
